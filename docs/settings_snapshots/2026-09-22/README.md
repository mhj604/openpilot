# C2 현재 설정 기록

2026-09-22 C2에 읽기 전용 SSH로 접속해 수집한 사용자 설정 202개입니다.
정확한 수집 시각(UTC), 실행 소스 커밋 및 브랜치는 `c2-settings.json`에 기록했습니다.

- C2 실행 커밋: `4caf69deadb0f6a14691d09f80a941bd6b49da7b`
- C2 브랜치: `grandeur_ig_hev_nomfc`
- 값은 표시 단위로 환산하지 않은 Params 원본 문자열입니다.
- manager 초기 설정 목록의 숫자·숫자 배열과 일부 비민감 열거값을 선별했습니다.
- 인증키, 토큰, 계정 정보, 차량 식별정보, 경로 목록, 학습·실행 캐시는 포함하지 않았습니다.
- 이 파일은 전체 Params 복원 이미지가 아니며, apilot에 일괄 적용할 수 있는 설정 파일도 아닙니다. 이식 시 이름·단위·의미·사용처를 별도로 대조해야 합니다.
- 여러 파일을 실행 중 순차적으로 읽은 기록으로, 원자적 스냅샷은 아닙니다.
- 수집 과정에서 C2 설정이나 코드를 변경하거나 재부팅하지 않았습니다.

이전 같은 날 백업과 달리 현재 `OpkrMonitoringMode`는 `0`, `SpeedLimitDecelOff`는 `1`입니다.
최대 조향 요청 관련 `SteerMaxBaseAdj`, `SteerMaxAdj`, `MaxSteer`는 모두 `384`입니다.
이 커밋에는 로컬에 남아 있는 MDPS 오류 인식 코드 수정은 포함하지 않습니다.
